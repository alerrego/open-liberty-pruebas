version: "3.8"

services:
  mariadb-auth:
    image: mariadb:latest
    container_name: auth-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: auth_db
      MYSQL_USER: user_auth
      MYSQL_PASSWORD: pass_auth
    ports:
      - "3306:3306"
